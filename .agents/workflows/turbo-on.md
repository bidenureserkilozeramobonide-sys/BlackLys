---
description: Active le mode turbo - toutes les commandes s'exécutent automatiquement sans approbation
---

// turbo-all

1. Mettre le contenu du fichier `.agents/turbo_mode.md` à `ON`
2. Confirmer que le mode turbo est activé
